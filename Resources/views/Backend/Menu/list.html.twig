{% extends '@CMS/Backend/Entity/List/list.html.twig' %}

{% block content_top_actions_create %}
    {% if is_granted(permissions.create) and 'create' in actions %}
        <div class="col-auto content-actions">
            <a class="btn btn-sm btn-primary" href="{{ path('cms_menu_create') }}">
                <span class="fa fa-plus"></span> {{ 'admin.btn.addMenu'|trans({}, 'cms') }}
            </a>
        </div>
    {% endif %}
{% endblock content_top_actions_create %}
