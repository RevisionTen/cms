{% extends '@cms/EasyAdmin/default/list.html.twig' %}

{% block global_actions %}
    {{- parent() -}}

    {% if is_granted('role_create') %}
        <div class="button-action">
            <a class="btn btn-success" href="{{ path('cms_role_create') }}">
                <span class="fa fa-plus"></span> {{ 'admin.btn.addRole'|trans({}, 'cms') }}
            </a>
        </div>
    {% endif %}
{% endblock global_actions %}
