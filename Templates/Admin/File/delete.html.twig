{% extends '@EasyAdmin/default/layout.html.twig' %}

{% block content_title %}
    {{ 'admin.label.fileDelete'|trans({'%title%': file.title}, 'cms') }}
{% endblock %}

{% block body_class %}files{% endblock %}

{% block main %}
    <div class="p-3">
        <p>
            {{ 'admin.label.fileDeleteQuestion'|trans({}, 'cms')|raw }}
        </p>
        <p>

        </p>
    </div>
{% endblock %}

{% block content_footer %}
    <a class="btn btn-danger"
       href="{{ path('cms_file_delete', {
        id: id,
        confirm: true
    }) }}">
        {{ 'admin.btn.deleteFile'|trans({}, 'cms')|raw }}
    </a>
{% endblock %}
