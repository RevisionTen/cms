{% extends '@EasyAdminBase/default/list.html.twig' %}

{% block global_actions %}
    {{- parent() -}}

    {% if is_granted('menu_create') %}
        <div class="button-action">
            <a class="btn btn-success" href="{{ path('cms_menu_create') }}">
                <span class="fa fa-plus"></span> {{ 'Add menu'|trans }}
            </a>
        </div>
    {% endif %}
{% endblock global_actions %}
