{% extends '@EasyAdmin/default/layout.html.twig' %}

{% block content_title %}
    {% if title is defined %}
        {{ title|trans }}
    {% endif %}
{% endblock %}

{% block main %}
    {{ form(form) }}
{% endblock %}
