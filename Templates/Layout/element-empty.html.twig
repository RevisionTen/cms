{% if edit %}
    <div class="editor-empty-element d-flex w-100 justify-content-center pt-3 pb-3">
        <div class="text-center text-muted p-2 rounded editor-border-dashed">
            {{ 'Empty %elementName%'|trans({'%elementName%': element.elementName|trans}) }}
        </div>
    </div>
{% endif %}
